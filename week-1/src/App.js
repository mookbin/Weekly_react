import React from "react";
import Layout from "Layout";

function App() {
  return (
    <Layout>
      <div>App 컴포넌트에서 보낸 값입니다.</div>
      {/* 여기에 어떤 걸 쓴다면 그게 children이 된다. */}
    </Layout>
  );
}

export default App;
